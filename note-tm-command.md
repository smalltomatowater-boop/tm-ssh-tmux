# SSH先で `tm` と打つだけにした話  
## `tmux` 運用の手間を減らす小さなコマンドを作った

リモート環境で作業していると、地味に面倒なのが `tmux` セッションの出入りです。  
朝イチで SSH して最初にやるのが `tmux attach -t ...`。これを何度もやるのが、正直ずっと嫌でした。

そこで、SSH先で `tm` と打つだけでセッション選択や作成ができる小さなコマンドを作りました。  
この記事では、実装した背景と、実際に何ができるようになったかを書きます。

## これを作った背景

きっかけは単純で、作業環境を `tmux` に寄せて統一したかったからです。  
Codex でも Claude Code でも、結局サーバー側は同じ `tmux` セッションで回したい。  
アプリや端末が違っても運用を変えないようにしたかった、というのが一番大きい理由です。

もうひとつは、入力コストの削減です。  
`ssh mini` してから `tmux` の長いコマンドを打つ流れを、`tm` の1文字に寄せました。  
iPad / iPhone から SSH する時も、この差は体感で大きいです。

最初は shell alias でしのいでいましたが、`tmux` の内外で挙動を分ける必要が出てきて、単体コマンドに切り出しました。

## `tm` でできること

使い方は3つだけです。

```bash
tm
```

既存セッションを一覧表示し、番号で選んで接続できます。  
`n` で新規作成、`q` で終了です。

```bash
tm work
```

`work` セッションへ接続します。なければ作成されます。  
つまり「attach か create か」を毎回考えなくてよくなります。

さらに、すでに `tmux` の中で `tm` を実行した場合も、そのまま別セッションへ移動できます。  
ネストせず `switch-client` で切り替える実装にしています。

## 実装でやったこと（シンプル版）

中身は Bash の単一スクリプトです。大きくは次の4点です。

1. `tmux` の存在確認  
`TMUX_BIN` 環境変数、または `command -v tmux` で実行ファイルを解決し、なければ終了。

2. セッション名の最低限サニタイズ  
スペース、`:`、`.` を `-` に置換して扱いやすくしています。

3. 接続ロジックの分岐  
`tmux` 外なら `new-session -A -s <name>` で attach-or-create。  
`tmux` 内なら `switch-client` を使ってセッションを切り替えます。

4. 対話モード  
`list-sessions` の結果を読み、番号選択で接続。  
セッションが1つもなければ、その場で新規名を聞いて作成。

逆に、あえて入れていないものもあります。  
例えば fuzzy finder 連携や複雑な命名ルールです。ここは「どこでも動く」を優先して Bash 単体に寄せました。

## 実際の運用で変わったこと

いちばん変わったのは、セッション操作の「迷い」が減ったことです。  
`attach` するのか、`create` するのか、今 `tmux` の内側か外側か、を意識しなくて済みます。

普段の流れはこんな感じです。

```bash
ssh mini
tm
```

または、1発で入るなら:

```bash
ssh mini -t tm
```

小さい改善ですが、毎日触るところを短くすると効きます。  
「便利機能を増やす」より、「迷わない入口を作る」方向の実装でした。

## まとめ

`tm` は機能が多いツールではありません。  
でも、毎日いちばん最初に触る操作が短くなるだけで、作業に入るまでの摩擦はかなり減りました。  
セッション管理を手癖にしたいなら、こういう薄いラッパーは相性がいいです。
